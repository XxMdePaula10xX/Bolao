/* ============================================================
   CONFIGURAÇÃO DO FIREBASE (web)
   ------------------------------------------------------------
   1. Copie este arquivo para  firebase-config.js  (mesma pasta).
   2. Cole os valores do seu projeto (os MESMOS do arquivo .env
      do app React Native — Console do Firebase > Config. do
      projeto > Seus apps > app Web).
   São chaves públicas do cliente (podem ir pro navegador).
   ============================================================ */
export const firebaseConfig = {
  apiKey: 'COLE_AQUI',
  authDomain: 'seu-projeto.firebaseapp.com',
  projectId: 'seu-projeto',
  storageBucket: 'seu-projeto.appspot.com',
  messagingSenderId: 'COLE_AQUI',
  appId: 'COLE_AQUI',
};
