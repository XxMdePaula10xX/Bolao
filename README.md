# Recomeço

Repositório reiniciado do zero.

> O trabalho anterior (app React Native + web app + Capacitor/Codemagic +
> backend Firebase) continua no histórico do Git, na tag local `arquivo-v1`
> e no commit `614ff0b`. Para recuperar qualquer arquivo:
>
> ```bash
> git checkout arquivo-v1 -- caminho/do/arquivo
> ```
